version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "../../migrations"
    gen:
      go:
        emit_interface: true
        emit_json_tags: true
        package: "db"
        sql_package: "pgx/v5"
        out: "."